# OKOME cache nodes – journald RAM-only (SD wear reduction)
# Copy to /etc/systemd/journald.conf.d/okome.conf
# Restart: sudo systemctl restart systemd-journald

[Journal]
Storage=volatile
RuntimeMaxUse=50M
RuntimeMaxFileSec=1day
