# OKOME Frontend – hardened (stub)
# Phase 2. Health-gated failover, /etc/nginx/okome/health_state.conf, observability.

# include /etc/nginx/okome/health_state.conf;
# Build on okome-frontend.conf.
