# OKOME cache nodes – fstab hardening (SD wear reduction)
# Merge relevant lines into /etc/fstab; keep existing mounts.
# Plan: okome_two-node_cache_architecture_implementation
#
# Root: add noatime, commit=60 to existing line (edit manually).
# Example (do not copy verbatim):
#   /dev/mmcblk0p2  /  ext4  noatime,commit=60,errors=remount-ro  0  1
#
# Optional tmpfs for Nginx cache (frontend only):
# tmpfs /var/cache/nginx tmpfs size=512m,noatime 0 0
#
